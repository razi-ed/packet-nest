# Environment variables should ideally not be tracked in version control (e.g., Git) 
# to avoid exposing sensitive data such as API keys, passwords, or other secrets.
# This file is included as an exception to simplify project setup and running.

VITE_APP_NAME="PacketNest"
VITE_API_BASE_URL="/api"
VITE_ITEMS_PER_PAGE="5"

# Only entries starting with 'VITE_' will be bundled into the client-side code.
# Variables without the 'VITE_' prefix remain server-side only.
LIBRARIES_BASE_URL="https://libraries.io"
LIBRARIES_API_KEY="da5a254ed6d9bcad38516250c952256f"