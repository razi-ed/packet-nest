export const moduleName = "package";

const linkLabel = "Package";
const routePath = `${moduleName}`;
const linkKey = `${moduleName}`;

export const PackageNavObject = {
  routePath,
  linkLabel,
  linkKey,
};

export default PackageNavObject;
